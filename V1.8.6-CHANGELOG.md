# บ้านผีสิง V1.8.6

ฐาน: V1.8.5 commit a758a7d7fe978c9546bd44277a037998313d79af

1. หยิบการ์ดจากกองทิ้งเข้ามือ: ไม่เสนอ Event และเซิร์ฟเวอร์ปฏิเสธการส่ง UID ของ Event โดยตรง หากกองทิ้งมีแต่ Event จะไม่เปิดหน้าต่างเปล่า ถ้ามือเต็มยังเลือกทิ้งจากทั้ง 6 ใบได้ ผลนี้ใช้กับระบบหยิบคืนที่มีอยู่ (ห้องทำงาน); กองเครื่องเซ่นนอกเกมไม่มี Event อยู่แล้ว ไม่ได้เพิ่มแหล่งหยิบหรือเปลี่ยนกติกาจั่วจากกองปกติ
2. Game Log: กดแต่ละบรรทัดเพื่อดูข้อความเต็ม สาเหตุ ผู้รับผล การเปลี่ยน HP/เงิน/ตำแหน่งที่บันทึก และกติกาการ์ดที่ถูกเอ่ยถึง เก็บล่าสุด 120 รายการต่อห้อง หน้าต่างอ่านแยกจากหน้าต่างตัดสินใจ ไม่ข้ามสิ่งที่ต้องเลือก ไม่บันทึกไพ่ลับหรือ token ลงรายละเอียดสาธารณะ; Log เป็นข้อมูลของห้องระหว่างเซิร์ฟเวอร์ทำงาน ยังไม่ใช่ประวัติถาวรของบัญชี
3. ลูกเต๋า 3D: ลูกบาศก์ CSS หกหน้าพร้อมจุด หมุนก่อนหยุดตามค่าจากเซิร์ฟเวอร์ แล้วแสดงผลรวมเดิม ไม่เปลี่ยนการสุ่มหรือกติกา รองรับการลดแอนิเมชันของระบบ
4. หมากเดิน: รูปคนมินิมอลแบบ SVG ใหญ่ขึ้น ใช้สีเดิม ไม่มี P1/P2 บนตัวหมาก มีชื่อสีและหมายเลขในพื้นที่ของฉันและคำอธิบายเมื่อชี้หมาก
5. เพลงใหม่: Lo-Fi เที่ยงคืน (74 BPM), โคมผี (86 BPM), ฝนหลอน (66 BPM) สังเคราะห์จาก Web Audio เป็นลูปต้นฉบับ มี kick/snare/hi-hat เบส คอร์ดไมเนอร์และทำนอง ไม่โหลดไฟล์เพลงภายนอก ใช้ตัวปรับเสียงเดิม และยกเลิกจังหวะ/ตัดเสียงเพลงเก่าเมื่อสลับหรือปิด
6. ไฮไลท์ก่อนยืนยันสติ: ใช้การคำนวณเส้นทางเดียวกับการเดินจริง แสดงห้องที่เข้าได้ทันทีหลังทอยและเปลี่ยนตามการ์ดปรับสติ กรอบประเป็น preview ยังต้องกดยืนยันก่อนเดิน ไม่เปลี่ยนระยะทางหรือข้อจำกัด Fear/ความจุ/ห้องพิเศษ
7. ช่องอุปกรณ์: พ่อไกร 3 ช่อง ตัวละครอื่นทั้ง 7 ตัว 2 ช่อง อิงข้อมูลเดียวกันทั้ง UI และเซิร์ฟเวอร์ รวมกรณีสวมฟรีจากห้องน้ำ
8. Amulet บนมือ: ขยายจาก 96×134 เป็น 124×174 px บนจอใหญ่ และ 110×155 px บนจอเล็ก ปรับระยะพัดไพ่ตามความกว้างหน้าจอ เก็บลำดับชื่อ/ภาพ/ข้อมูลเดิม
9. หัวกระดาน: “บ้าน 3×3” เปลี่ยนเป็น “บ้านผีสิง”

## ทดสอบ

- `node --check server.js`, `node --check public/app.js`, `git diff --check`
- `npm test`: regression V1.8, V1.8.5 server/client และ V1.8.6 server/client ผ่าน
- V1.8.6 ทดสอบการกรอง/ปฏิเสธ Event, กองมีแต่ Event, หยิบตอนมือเต็ม, เพดานอุปกรณ์ทุกตัว, preview ไม่ทำให้เดินได้ก่อนยืนยันและตรงกับผลหลังยืนยัน, log รายละเอียดไม่เปิดไพ่ลับ, หน้าลูกเต๋าทั้ง 6 และผลเซิร์ฟเวอร์, ข้อความ Log ไม่แปลเป็น HTML, เพลงทั้ง 3 แบบมี timer เดียวและล้างเมื่อสลับ
- การทดสอบ client ใช้ DOM/audio จำลอง ยังต้องตรวจภาพ เสียง และเล่นหลายคนบนเว็บทดสอบหลัง deploy
